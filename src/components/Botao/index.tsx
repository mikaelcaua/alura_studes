import React from "react";
import './style.scss'

export default class Botao extends React.Component{
    render() {

        return(
            <button className="botao">
                Botao
            </button>
        )
    }
}